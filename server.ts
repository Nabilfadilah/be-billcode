// const express = require("express"); // Impor modul Express
// const app = express(); // Buat instance aplikasi Express
// const port = 3500; // Tentukan port yang akan digunakan

// // Middleware untuk parsing request body (opsional, untuk POST/PUT request)
// app.use(express.json());
// app.use(express.urlencoded({extended: true}));

// // Rute root (GET request ke '/')
// app.get("/", (req = "", res = "") => {
//   res.json({message: "Halo, ini adalah backend pertama Anda!"});
// });

// // Mulai server dan dengarkan di port yang ditentukan
// app.listen(port, () => {
//   console.log(`Aplikasi berjalan di http://localhost:${port}`);
// });
